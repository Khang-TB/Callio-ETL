# BigQuery credentials (choose one of the following options)
# SERVICE_ACCOUNT_KEY_FILE=./service-account.json
SERVICE_ACCOUNT_KEY_JSON={"type": "service_account", "project_id": "your-project", "private_key_id": "...", "private_key": "-----BEGIN PRIVATE KEY-----\\n..."}

# Callio tenant credentials (multi-tenant supported)
# Alternatively point CALLIO_ACCOUNTS_FILE to a JSON file with the same structure.
CALLIO_ACCOUNTS_JSON=[
  {"tenant": "tenant-a", "email": "user-a@example.com", "password": "changeme"},
  {"tenant": "tenant-b", "email": "user-b@example.com", "password": "changeme"}
]

# Optional overrides
# BQ_PROJECT_ID=your-project-id
# BQ_DATASET_ID=your_dataset
# BQ_LOCATION=asia-southeast1
# API_TIMEOUT=90
# API_PAGE_SIZE=500
# LOG_LEVEL=INFO
# LIMIT_RECORDS_PER_ENDPOINT=1000
